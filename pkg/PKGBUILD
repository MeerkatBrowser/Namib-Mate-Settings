pkgname=namib-mate-settings
pkgdesc='Settings file for Namib Mate'
pkgver=18.02
pkgrel=1
arch=('any')
url="https://github.com/MeerkatBrowser/namib-mate-settings"
license=('GPL')
makedepends=('git')
replace=('mate-backgrounds')
source=("git+$url.git")
sha256sums=('SKIP')
install=$pkgname.install

package() {
    package_path="/etc/$pkgname"
    cd "${srcdir}/${pkgname}"

	install -d ${pkgdir}/etc/lightdm/
    install -m644 etc/lightdm/* "${pkgdir}/etc/lightdm/"

	#-------------------------glib config files----------------------
    install -d ${pkgdir}/usr/share/glib-2.0/schemas/
    install -m644 usr/share/glib-2.0/schemas/* "${pkgdir}/usr/share/glib-2.0/schemas/"
    
    #-------------------------mate-panel config files----------------------
    install -d ${pkgdir}/usr/share/mate-panel/layouts/
    install -m644 usr/share/mate-panel/layouts/* "${pkgdir}/usr/share/mate-panel/layouts/"

	install -d ${pkgdir}/etc/skel_mate
    cp -r ${srcdir}/namib-mate-settings/etc/skel_mate/ "${pkgdir}/etc/"
    
    install -d ${pkgdir}/usr/share/backgrounds/namib/config
    install -m644 mate-backgrounds.xml "${pkgdir}/usr/share/backgrounds/namib/config"
}
